import 'package:flutter/material.dart';

class RecentWidget extends StatelessWidget {
  const RecentWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Recent sheet"),
    );
  }
}
