import 'package:flutter/material.dart';

class FansWidget extends StatelessWidget {
  const FansWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Fans sheet"),
    );
  }
}
